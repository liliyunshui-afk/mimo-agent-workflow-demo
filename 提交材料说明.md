# 第 5 项可提交材料说明

## 2. 终端运行日志或 Agent 工作流截图/录屏

推荐上传以下任一文件：

- `artifacts/terminal_run_log.txt`
- `artifacts/workflow_result.json`

本地还生成了以下图片，可直接在申请页面上传：

- `第2项_Agent工作流截图.png`
- `第2项_终端运行日志截图.png`

其中 `terminal_run_log.txt` 是实际运行 `workflow_demo.py` 生成的本地 MVP 日志；截图文件用于上传框要求图片格式时使用。

## 3. GitHub 项目链接或产品在线演示地址

GitHub 项目链接：

```text
https://github.com/liliyunshui-afk/mimo-agent-workflow-demo
```

GitHub Pages 在线演示地址预计为：

```text
https://liliyunshui-afk.github.io/mimo-agent-workflow-demo/
```

如果 Pages 页面暂时打不开，请在 GitHub 仓库页面进入 `Settings -> Pages`，选择 `Deploy from a branch`，分支选择 `main`，目录选择 `/root`，保存后等待 1 到 3 分钟。

## 说明

当前版本是 MVP dry-run，用于展示 Agent 编排逻辑和可验证产物格式。接入真实 MiMo API 后，可以继续补充脱敏 API 请求日志、token 消耗统计截图和模型评测案例。
